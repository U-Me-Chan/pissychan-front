div.pagination
  each page in pages
    if (page - 1) * limit == offset
      a(class='page page-selected') <b>#{page - 1}</b>
    else
      a(class='page' href='/' + tag + '?limit=' + limit + '&offset=' + (page - 1) * limit) #{page - 1}